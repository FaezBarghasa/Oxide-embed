#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    # Stop user service if active
    if command -v systemctl >/dev/null 2>&1; then
        systemctl --user stop oxide-watch.service 2>/dev/null || true
    fi
fi

exit 0
